#include <stdio.h>
#include <conio.h>//this is for only windows os. so not used in linux, for linux cursor.h file is there

void main()
{
	printf("Hello C");//this function is present in stdio.h file.Purpose is to show output
	getch();//this function is present in conio.h. Purpose is to get character but does not show
}
